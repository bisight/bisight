{% extends "@Theme/layout.html.twig" %}

{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block content %}
    <h1>
        {% block header %}
            Configure warehouse
        {% endblock %}
    </h1>

    {% block form %}
        {{ form_start(form) }}
            <div class="buttonbox">
                <a href="warehouse_index') }}" class="btn btn-default" role="button">
                    <i class="fa fa-arrow-left"></i>
                    Back
                </a>

                {% block buttons %}
                {% endblock %}

                <button type="submit" class="btn btn-success">
                    <i class="fa fa-save"></i>
                    Save
                </button>
            </div>

            {{ form_widget(form) }}
        {{ form_end(form) }}
    {% endblock %}
{% endblock %}
