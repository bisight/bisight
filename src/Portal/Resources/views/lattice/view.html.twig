{% extends "layout.html.twig" %}

{% block content %}
<div style="float: right; margin-top: 20px;">
    <a href="{{ path("lattice_download", { dwcode: datawarehouse.getCode(), latticecode: lattice.getName() })}}?format=csv" class="btn btn-default"><i class="fa fa fa-file-text-o"></i> CSV</a>
    <a href="{{ path("lattice_download", { dwcode: datawarehouse.getCode(), latticecode: lattice.getName() })}}?format=html" class="btn btn-default"><i class="fa fa fa-file-code-o"></i> HTML</a>
    <a href="{{ path("lattice_download", { dwcode: datawarehouse.getCode(), latticecode: lattice.getName() })}}?format=xlsx" class="btn btn-default"><i class="fa fa fa-file-excel-o"></i> Excel</a>
</div>
<h1><i class="fa fa-table"></i> Lattice "{{ lattice.getName() }}"</h1>
<p>Row count: {{ rowcount }}</p>
{{ tablehtml|raw }}

{% endblock %}
